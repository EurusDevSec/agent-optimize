# BENCHMARK TASK 01: State Mutation Repair

## Goal
Fix a subtle state mutation bug where array mutation leaks state across requests.

## Target File
`src/user_service.py`

## Test Command
`python -m unittest discover -s tests`
